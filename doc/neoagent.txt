*neoagent.txt*  LLM and coding-agent tools for Neovim

===============================================================================
CONTENTS                                                        *neoagent-contents*

1. Introduction                                      |neoagent-introduction|
2. Setup                                                    |neoagent-setup|
   Built-in providers                         |neoagent-built-in-providers|
   Controllers and Views                           |neoagent-controllers|
   Workspace trust                             |neoagent-workspace-trust|
   Sandboxing                                            |neoagent-sandbox|
   AGENTS.md and skills                             |neoagent-context-files|
   Provider services                             |neoagent-provider-services|
   Workspace settings                             |neoagent-workspace-settings|
3. Commands                                              |neoagent-commands|
4. UI                                                          |neoagent-ui|
   Renderers                                          |neoagent-renderers|
5. Core APIs                                              |neoagent-core-api|
6. Tools and sessions                               |neoagent-composition|
   Context compaction                              |neoagent-compaction|

===============================================================================
1. INTRODUCTION                                           *neoagent-introduction*

Neoagent provides streamed Models, cancellable Runs, and a plain agent loop.
The plugin adds optional tools, persistence, provider management, and a
floating UI.

Requirements:

  Neovim 0.10+
  curl 7.76+
  rg
  fd

ImageMagick's `magick` is optional. Run this after installation: >

  :checkhealth neoagent

===============================================================================
2. SETUP                                                         *neoagent-setup*

A minimal setup selects a model. Neoagent installs no global mappings: >lua

  require("neoagent").setup({
    default_model = {
      provider = "openai",
      model = "gpt-5.4",
    },
  })

  vim.keymap.set("n", "<leader>a", "<cmd>Neoagent<cr>")
<
For a local OpenAI-compatible endpoint: >lua

  require("neoagent").setup({
    providers = {
      local_llama = {
        api = "openai-completions",
        base_url = "http://127.0.0.1:8080/v1",
        models = {
          coder = {
            input = { "text" },
            context_window = 131072,
            max_output_tokens = 8192,
          },
        },
      },
    },
    default_model = { provider = "local_llama", model = "coder" },
  })
<
The main options and defaults are: >lua

  {
    name = nil,
    default_registry = true,
    providers = {},
    apis = {},
    auth = { path = ..., methods = {} },
    default_model = nil,
    default_thinking_level = "medium",
    system_prompt = nil,
    tools = nil,
    shell_timeout = 300,
    execute_tool = nil,
    interaction = nil,
    view = nil,

    retry = {
      enabled = true,
      max_retries = 3,
      base_delay_ms = 2000,
    },
    compaction = {
      auto = true,
      reserve_tokens = 16384,
      keep_recent_tokens = 20000,
      run = nil,
    },
    sandbox = {
      enabled = false,
      profile = nil,
    },
    workspace_trust = {
      path = stdpath("state") .. "/neoagent/trust.json",
    },
    agents = {
      global_files = { stdpath("config") .. "/AGENTS.md" },
      project_filenames = { "AGENTS.md" },
    },
    skills = {
      global_dirs = {
        expand("~/.agents/skills"),
        stdpath("config") .. "/neoagent/skills",
      },
      project_dirs = { ".agents/skills" },
    },
    persistence = {
      enabled = true,
      workspace_settings = true,
      directory = stdpath("state") .. "/neoagent/workspaces",
    },
    ui = {
      style = "codex",
      renderer = nil,
      position = "center",
      width = nil,
      height = nil,
      margin = 1,
      input_height = 7,
      scroll_on_submit = true,
      scroll_on_transcript_leave = true,
      scroll_on_reopen = true,
      wrap_cards = false,
      show_thinking = true,
      provider_console = {
        position = "top",
        width = 0.4,
        height = 0.35,
      },
      completion = { sources = { "files" } },
      border = "rounded",
      mappings = {},
    },
  }
<
Provider and model tables recursively override the built-in registry. Assign
`false` to remove an entry, or set `default_registry = false` to use only
your own providers. A model may set `api` to override its provider's adapter.
Built-in adapter names are `openai-completions`, `openai-responses`,
`openai-codex-responses`, and `anthropic-messages`.

Providers, models, and individual Model calls may set `request_opts` to a
table or callback. The layers merge in that order across `url`, `headers`,
and `body`. Header names are case-insensitive, maps merge recursively, and
lists replace inherited lists.

A model's `thinking` table maps levels to request options. Levels are ordered
`off`, `minimal`, `low`, `medium`, `high`, `xhigh`, and `max`.
Missing levels are unavailable; `false` removes an inherited level. Selecting
a model starts at `default_thinking_level`, clamped to a supported value.

`input` lists accepted message modalities: `"text"`, `"image"`, or both.
Built-in adapters replace unsupported images in the request copy with a text
placeholder. Sessions keep the original image block.

The built-in system prompt follows the active tools and includes the working
directory. Set `system_prompt` to a string or callback to replace it. The
callback receives the Session, Model, Workspace, submitted prompt, tools, and
discovered resources. Configured AGENTS.md and skill context is appended to
the result.

BUILT-IN PROVIDERS                              *neoagent-built-in-providers*

The default provider IDs are:

  openai             OpenAI API; login `openai` or `OPENAI_API_KEY`.
  openai-codex       ChatGPT subscription; login `openai-codex`.
  anthropic          Anthropic API; login `anthropic` or
                     `ANTHROPIC_API_KEY`.
  anthropic-plan     Claude subscription; login `anthropic-plan` or
                     `ANTHROPIC_OAUTH_TOKEN`.
  deepseek           DeepSeek API; login `deepseek` or `DEEPSEEK_API_KEY`.
  zai                Z.AI API; login `zai` or `ZAI_API_KEY`.
  zai-coding-plan    Z.AI Coding Plan using the `zai` credential.
  opencode-go        OpenCode Go; login `opencode-go` or
                     `OPENCODE_API_KEY`.
  llama.cpp          Local llama.cpp router; login `llama`.

Use `:NeoagentLogin [method]` to store a key or start OAuth.
`:NeoagentLogout [method]` removes the stored credential. Stored credentials
take precedence over environment variables.

Credentials live at `stdpath("state") .. "/neoagent/auth.json"`. The store
uses atomic writes, private permissions, and a cross-process lock. Listing
credentials returns IDs and types only.

Codex diagnostics default to
`stdpath("state") .. "/neoagent/codex.log"`. They contain bounded transport
metadata and exclude credentials, request and response bodies, and conversation
content. Configure or disable the sink under the provider: >lua

  providers = {
    ["openai-codex"] = {
      diagnostics = false,
      -- diagnostics = { path = "/private/path/codex.log" },
    },
  }
<
CONTROLLERS AND VIEWS                                *neoagent-controllers*

`setup(opts)` creates the built-in Neo and Chat Controllers in one Window and
returns Neo. Neo uses the coding prompt, resources, and tools. Chat has an empty
system prompt and tool list. Top-level functions and commands target the active
Controller.

`new(opts[, runtime])` creates an independent Controller. Runtime policies,
such as workspace trust and shared Provider Services, are passed separately
from configuration: >lua

  local neoagent = require("neoagent")
  local coding = neoagent.setup()
  local review = neoagent.new({
    name = "Review",
    tools = require("neoagent.tools").read_only(),
    persistence = { enabled = false },
    system_prompt = "Review this project.",
  })
  local window = neoagent.new_window({
    controllers = { coding, review },
    ui = { position = "left" },
  })
  neoagent.set_default_window(window)
<
Controller methods include: >lua

  controller:prepare()
  controller:send(text)
  controller:steer(text)
  controller:dequeue_steering()
  controller:compact([instructions])
  controller:stop()
  controller:new_session()
  controller:resume([path[, callback]])
  controller:branch(entry_id[, summary])
  controller:select_branch([callback])
  controller:fork([entry_id[, position]])
  controller:select_fork([callback])
  controller:select_model([callback])
  controller:set_model(provider, model)
  controller:available_thinking_levels()
  controller:get_thinking_level()
  controller:set_thinking_level(level)
  controller:cycle_thinking_level()
  controller:login([method])
  controller:cancel_login()
  controller:logout([method])
  controller:provider_operations()
  controller:provider_info()
  controller:provider_operation(operation[, args])
  controller:cancel_provider()
  controller:set_ui_position(position)
  controller:subscribe(callback)
  controller:snapshot()
  controller:get_session()
  controller:get_model()
  controller:get_workspace()
  controller:get_toolset()
  controller:set_toolset(toolset)
  controller:config()
  controller:is_running()
  controller:is_authenticating()
  controller:destroy()
<
`send()` starts a turn while idle and queues steering while a Run is active.
Each Controller owns its Session, draft, model selection, toolset, steering
queue, and Run. `set_toolset()` replaces the `{ tools, execute_tool }` pair
between Runs and returns the previous value. A Run keeps the toolset it started
with.

A Window owns one View and requires unique, non-empty Controller names. Its
public operations are `open`, `close`, `toggle`, `is_open`, `active`,
`controllers`, `select`, `cycle`, `set_position`, `set_renderer`,
`set_transcript_style`, `set_input`, `get_input`, `view`,
`rendered_controller`, `input_history`, `add_input_history`,
`select_input_history`, `set_provider_console`,
`toggle_provider_console`, `provider_console_open`, and `destroy`.
Inactive Controllers continue running.

`set_default_window(window)` installs the command-facing Window.
`default_window()` and `default()` return that Window and its active
Controller. `set_default(controller)` creates a one-Controller default
Window. Replacing a setup-owned composition destroys the previous owned
Controllers; an active owned Run blocks another `setup()`.

`controller:subscribe(callback)` publishes `messages`, `context`, `event`,
and `finish` updates and returns an unsubscribe function. `snapshot()`
provides the current state to a newly attached consumer.

A custom View factory receives the resolved UI configuration, its Window,
callbacks for user actions, and active-tool resolution. It returns a passive
View implementing `open`, `close`, `is_open`, `destroy`, `get_input`,
`set_input`, `set_messages`, `set_context`, `apply`, and `finish`.
Renderer replacement, dialogs, and provider console support use the optional
`set_renderer`, `set_dialog`, `set_provider`, and `set_provider_open`
methods.

Executors may publish dialogs through an injected `ctx.dialog` capability.
`dialog:show(request)` returns a cancellable Run. Requests choose
`placement = "transcript"` or `"float"`, define actions, and may include an
editable input. Dialogs are FIFO and may be scoped to a Controller name.

WORKSPACE TRUST                                  *neoagent-workspace-trust*

The built-in Neo Controller asks for trust before it loads project resources
or starts a tool-capable Run. Chat and direct Lua compositions use only
policies supplied by their caller.

The trust target is the canonical Git worktree root, or the canonical working
directory outside Git. The prompt offers persistent trust, trust until Neovim
exits, and cancel (`t`, `s`, and `q`). Accepting trust leaves the draft ready
for explicit submission. Closing the Window leaves the decision unresolved.

Persistent decisions default to
`stdpath("state") .. "/neoagent/trust.json"`. The file contains canonical
trusted paths and is updated atomically with private permissions. Remove a path
from that file to revoke trust. Configure another path or disable the built-in
policy with: >lua

  workspace_trust = { path = "/private/path/trust.json" }
  -- workspace_trust = false
<
Trust allows repository content to influence Neo. Executor policy and sandbox
configuration control tool effects.

Custom compositions can use `neoagent.workspace_trust.compose(opts, runtime)`.
It returns a View factory and policy. Pass the policy to
`neoagent.new(opts, { workspace_trust = policy })`, pass the View and shared
dialog source to the Window, then call `policy:attach_window(window,
controller)`.

SANDBOXING                                                 *neoagent-sandbox*

Native sandboxing is experimental and disabled by default. Enable its initial
state for the built-in Neo Controller with: >lua

  require("neoagent").setup({
    sandbox = { enabled = true },
  })
<
Use `:NeoagentToggleSandbox` to change the current editor state and
`:NeoagentSandboxInfo` to inspect the backend and capabilities. Each tool call
keeps the mode it started with. Runtime changes preserve the tool list and
schemas.

The default profile grants read-only host access, writes within the Workspace
and shared temporary directory, read-only access to `.git`, restricted
networking, and a small inherited environment. Override it with a table or a
callback: >lua

  sandbox = {
    enabled = true,
    profile = {
      filesystem = {
        entries = {
          { path = vim.uv.cwd(), access = "write" },
          { path = vim.fs.joinpath(vim.uv.cwd(), ".git"), access = "read" },
          { path = "/absolute/secret", access = "deny" },
        },
      },
      network = "restricted",
    },
  }
<
Filesystem access is `read`, `write`, or `deny`; paths are canonical and
absolute. `network` is `restricted` or `enabled`. Lists replace their
defaults. A profile callback receives a copy of the default profile and the
tool context, then returns the complete profile.

Linux uses namespaces and seccomp. macOS uses
`/usr/bin/sandbox-exec`. Windows uses restricted accounts, ACLs, Job Objects,
and WFP and requires Neovim 0.12+. Provision Windows once from an elevated
PowerShell in the plugin checkout: >

  nvim --headless -u NONE -i NONE -n -l scripts/sandbox_windows_runtime.lua -- --setup
<
Set `NEOAGENT_WINDOWS_SANDBOX_STATE` before Neovim starts to choose another
state directory.

Custom tools participate when they route file and process effects through
`ctx.fs` and `ctx.process`. Tool Lua itself runs in Neovim.

A denied call can request one host execution by adding these tool arguments:
>json

  {
    "options": {
      "require_escalation": true,
      "escalation_justification": "why host execution is required"
    }
  }
<
Approval grants host capabilities for that call. Shell approvals may remember
a literal argument-token prefix for the current Session. Remembered rules are
held in memory and apply only to later calls that explicitly request
escalation. Compound or dynamic shell syntax always prompts.

Activation failures report the failed stage. An initial failure uses the host
executor and is shown in the workspace trust prompt or a UI warning. Failures
after successful activation return tool errors and do not fall back to host
execution.

Custom compositions can use
`require("neoagent.sandbox.composition").compose(toolset, config, runtime)`.
Use `.switchable()` when the caller needs runtime changes. Both return the
effective toolset and sandbox status; switchable composition also returns its
dialog source and runtime control.

Retries are configured separately under `retry`. Transient failures use
cancellable exponential backoff. Authentication, invalid-request, quota, and
billing failures return immediately. Context-window errors use compaction
recovery.

AGENTS.MD AND SKILLS                                  *neoagent-context-files*

Neo loads the global `stdpath("config") .. "/AGENTS.md"` and each project
`AGENTS.md` from the Git root to the Workspace. Outside Git, discovery starts
at the filesystem root. Files enter the prompt from broadest to most specific.

Skills are directories containing a `SKILL.md` with YAML `name` and
`description`. The defaults are:

  ~/.agents/skills
  stdpath("config") .. "/neoagent/skills"
  <project ancestor>/.agents/skills

Nearer project skills override broader or global skills with the same name.
Only metadata and the path enter the initial prompt; the model reads full
instructions on demand. Skill discovery requires an active file-reading tool.

Replace the configured lists to customize discovery. Empty lists disable those
locations. `agents = false` or `skills = false` disables that resource
type. The `neoagent.agents` and `neoagent.skills` modules also expose
`discover(opts)` and `format(resources)`.

PROVIDER SERVICES                                  *neoagent-provider-services*

A Provider Service supplies status, operations, and optional dynamic Models:
>lua

  {
    id = "example",
    name = "Example",
    state = function(ctx) ... end,
    operations = {
      refresh = {
        name = "Refresh",
        run = function(ctx) ... end,
      },
    },
  }
<
Optional members are `open_operation`, `get_models`,
`refresh_models(ctx)`, `refresh_catalog(opts)`, `wrap_model(model)`,
`subscribe(listener)`, `on_event(event)`, and `destroy()`.

Operations receive raw arguments, the selected model, agent-running state,
authentication resolution, and interaction helpers for selection, input,
confirmation, progress, and notifications. They return cancellable Runs. A
successful operation may return a bounded document artifact, which opens in a
scratch tab. Mutating operations are serialized and cannot overlap agent or
compaction use of the same service.

`neoagent.provider_state.new(snapshot)` creates a reusable dashboard channel.
Snapshots contain semantic `status`, `field`, `progress`, `limit`,
`list`, and `activity` blocks. The channel validates and copies updates and
delivers them on Neovim's main loop. Renderers decide layout and styling.

The built-in services are:

  openai-codex  Shows account limits and usage. Operations refresh usage, load
                activity and workspaces, inspect reset credits, and redeem a
                credit after confirmation.
  opencode-go   Shows shared subscription windows and a selected-model request
                estimate. `models` refreshes the public catalog.
  llama.cpp     Shows router, model, load, download, and request state.
                Operations are `catalog`, `refresh`, `load`, `unload`,
                `download`, and `preset`.

The provider console runs `open_operation` once when first opened. It performs
no background polling. Use `:NeoagentProvider operation [args]` to run another
operation and `:NeoagentProvider!` to cancel it.

OpenCode Go catalogs cache for one hour by default; llama.cpp catalogs cache
for 60 seconds. Set a provider's `catalog_cache = false` to disable persistence
or `{ ttl_ms = milliseconds }` to set another freshness window.

llama.cpp model definitions can name a Hugging Face source and router load
parameters: >lua

  providers = {
    ["llama.cpp"] = {
      models = {
        qwen = {
          hf_repo = "ggml-org/Qwen3.5-0.8B-GGUF",
          quantization = "Q4_0",
          load = { ctx_size = 32768, gpu_layers = 99 },
          context_window = 262144,
          max_output_tokens = 80000,
        },
      },
    },
  }
<
Configured definitions take precedence over router entries. The definition ID
is selectable and aliases its Hugging Face router ID. `service_opts` may set
`wait_timeout_ms`, `download_timeout_ms`, and `poll_interval_ms`.

WORKSPACE SETTINGS                                  *neoagent-workspace-settings*

With persistence and workspace settings enabled, model and thinking choices
are scoped by Controller name. UI position and the latest 100 accepted inputs
are shared by the Workspace. A resumed Session's recorded model and thinking
choice take precedence.

Set `persistence.workspace_settings = false` to keep configured model,
thinking, and position values authoritative while retaining Sessions and input
history.

The reusable settings object is created with
`require("neoagent.workspace_settings").new({ directory, root })`. It exposes
`metadata()`, `load()`, `merge(defaults)`, `write(settings)`, and
`update(patch)`.

===============================================================================
3. COMMANDS                                                   *neoagent-commands*

  :Neoagent                       Toggle the UI.
  :NeoagentCycle                  Select the next Controller.
  :NeoagentNew                    Start an empty Session.
  :NeoagentResume [path]          Resume a Session or open the selector.
  :NeoagentStop                   Cancel the active agent Run.
  :NeoagentModel [provider/model] Select a Model or open the selector.
  :NeoagentThinking [level]       Set or cycle the thinking level.
  :NeoagentPosition [position]    Set or select the UI position.
  :NeoagentTranscriptStyle style  Select the `pi` or `codex` Renderer.
  :NeoagentProvider [op [args]]   Toggle the console or run an operation.
  :NeoagentProvider!              Cancel the provider operation.
  :NeoagentLogin [method]         Store a key or authenticate an account.
  :NeoagentLogin!                 Cancel login.
  :NeoagentLogout [method]        Remove a stored credential.
  :NeoagentCompact [instructions] Compact the active branch.
  :NeoagentBranch [entry-id]      Move to an entry or select a branch point.
  :NeoagentFork [entry-id]        Fork before a user message or select one.
  :NeoagentToggleSandbox          Toggle sandbox execution for Neo.
  :NeoagentSandboxInfo            Show sandbox status and capabilities.

Argument-free selectors use `vim.ui.select`. Fork selection restores the
chosen user message to the input.

===============================================================================
4. UI                                                               *neoagent-ui*

The bundled UI has separate transcript and input windows. The transcript
supports normal search, motions, Visual selection, and yank. Streamed redraws
pause during operations that require stable text.

Input accepted while a Controller is busy becomes steering. Steering is sent
after the current assistant response and tool calls. The interrupt mapping
cancels the Run and restores queued steering to the draft.

Filename completion is available in Insert mode. Input history is shared by
Controllers in the Workspace and keeps the latest 100 accepted entries.

Default mappings:

  <CR>        Submit input, accept completion, or open card details.
  <Tab>       Open filename completion or select its next item.
  <C-c>       Clear a draft; with an empty draft, stop the active Run.
  <Esc><Esc>  Hide the UI from input Normal mode.
  <C-d>       Hide the UI when input is empty.
  <A-k>       Move to the previous card. `<C-Up>` is an alias.
  <A-j>       Move to the next card or input. `<C-Down>` is an alias.
  <C-w>j      Focus input.
  <C-w>k      Focus transcript.
  r           Toggle rendered and raw Markdown in text or thinking details.
  <S-Tab>     Cycle thinking levels.
  <A-n>       Cycle Controllers.
  <A-m>       Select a Model.
  <A-r>       Resume a Session.
  <Up>/<Down> Browse input history.
  <C-k>       Browse toward older input.
  <C-r>       Select from input history.
  <A-Up>      Restore queued steering to input.
  <A-p>       Toggle the provider console.
  <A-l>       Open or focus the provider console.
  <A-h>       Return from provider console to transcript.
  q           Hide the transcript or provider console.

Every `ui.mappings` value accepts a lhs string, a list of lhs strings, or
`false`. Mapping keys are `submit`, `complete`, `interrupt`, `close_input`,
`close_empty`, `card_details`, `card_raw`, `card_previous`, `card_next`,
`focus_input`, `focus_transcript`, `focus_provider`, `provider_back`,
`provider_input`, `provider_previous`, `provider_next`, `cycle_thinking`,
`cycle_agent`, `select_model`, `resume_session`, `history_previous`,
`history_next`, `select_history`, `dequeue_steering`, `toggle_provider`,
`provider_close`, and `close`.

`ui.position` accepts `auto`, `left`, `right`, `top`, `bottom`, or
`center`. Width and height accept a fraction in `(0, 1]`, an integer greater
than one, or nil for the layout default.

Set `ui.show_thinking = false` to hide reasoning from the transcript while
retaining it in the Session. Set `ui.wrap_cards = true` to soft-wrap complete
card lines. The default clipped view recomputes when the Window size changes.

RENDERERS                                                *neoagent-renderers*

A Renderer turns copied semantic values into declarative UI content. Codex is
the default; Pi is also bundled: >lua

  local renderers = require("neoagent.ui.renderers")
  renderers.codex
  renderers.pi
  renderers.get("codex")
  renderers.names()
<
Select one with `ui.style` or provide `ui.renderer`. The explicit Renderer
takes precedence. `window:set_renderer(renderer)` replaces the live Renderer
and redraws the current transcript.

A Renderer has a non-empty `name` and these required methods:

  render_block(block, opts)
  render_details(block, opts)
  render_dialog(snapshot, opts)

Optional methods are `define_highlights`, `render_focus`, `render_status`,
and `render_provider`.

Content presentations have this shape: >lua

  {
    lines = { "first line", "second line" },
    highlights = {
      { row = 0, col = 0, end_col = 5, group = "NeoagentAccent" },
    },
    line_groups = {
      [0] = "NeoagentUserBackground",
    },
  }
<
Rows and byte columns are zero-based. Renderer inputs are copies and contain no
buffers, windows, namespaces, extmarks, mutable View state, or response
callbacks. Rendering is synchronous.

`render_block` and `render_details` receive a `user`, `assistant`,
`thinking`, `tool`, `compaction`, or `notice` block plus bounded layout
context. Tool blocks may carry the active Tool's semantic `render` callback.
`render_dialog` formats dialog text and actions. `render_provider` maps
provider content rows to operation IDs. The View owns all Neovim resources,
focus, mappings, and action routing.

Codex uses compact transparent tool cards. Pi uses shaded, state-colored cards.
Both expose complete text, reasoning, tool output, and compaction summaries
through the details view. Text and thinking details can toggle rendered and raw
Markdown with the `card_raw` mapping.

===============================================================================
5. CORE APIS                                                   *neoagent-core-api*

Models can be constructed without `setup()`: >lua

  local model = require("neoagent.api.openai_completions").new({
    provider = "local",
    model = "coder",
    base_url = "http://127.0.0.1:8080/v1",
    api_key = nil,
    input = { "text", "image" },
    context_window = nil,
    max_output_tokens = nil,
    request_opts = nil,
  })

  local run = model:stream({
    messages = messages,
    system_prompt = nil,
    tools = {},
    request_opts = nil,
    on_event = function(event) end,
    on_done = function(result) end,
  })
<
Other constructors are `neoagent.api.anthropic_messages`,
`neoagent.api.openai_responses`, and
`neoagent.api.openai_codex_responses`. The Codex constructor also accepts
`responses_lite`, `request_max_retries`, and `on_diagnostic`.

Model events are `text_delta`, `thinking_delta`, `tool_call_delta`,
`usage`, and optional `provider_status`. A successful result has
`{ ok = true, message, text }`; failure has `{ ok = false, error, message? }`.
Partial output may appear in a failure message.

A Run supports: >lua

  run:cancel()
  run:is_done()
  run:is_cancelled()
  run:result()
  run:await()       -- inside neoagent.async.run()
<
Callbacks run on Neovim's main loop and completion occurs once.

The reusable agent loop is: >lua

  require("neoagent.agent").run({
    model = model,
    messages = messages,
    system_prompt = nil,
    tools = {},
    execute_tool = nil,
    context = nil,
    model_options = nil,
    get_steering_messages = nil,
    on_event = nil,
    on_done = nil,
  })
<
It copies messages and uses exactly the supplied Model, tools, executor, and
context. Malformed provider tool arguments become error tool results without
entering the executor.

Authentication is independently composable: >lua

  local manager = require("neoagent.auth").new({
    methods = { example = login_method },
    store = credential_store,
  })
  local authenticated = manager:wrap(model, "example")
<
Credentials are tagged `api_key` or `oauth` values. A login method may
provide `login`, `refresh`, `request_opts`, and `public_metadata`.
Stores implement `read`, `write`, `modify`, `list`, and `delete`.
`manager:list_credentials()` returns secret-free metadata.

Use `manager:wrap(model, id, { optional = true })` when an ambient credential
may run the Model without a stored login.

===============================================================================
6. TOOLS AND SESSIONS                                    *neoagent-composition*

A Tool is `{ name, description, input_schema, execute }`.
`execute(arguments, ctx)` returns
`{ content = { ... }, isError?, details? }`. Content blocks are UTF-8 text or
base64 images. The agent converts malformed results into tool errors.

`execute_tool(tool, arguments, ctx)` is the policy boundary for approval,
logging, sandboxing, and other decorators. Its context contains the Model, Run,
caller context, executor, update callback, and current tool call.

Optional Tool fields are:

  capabilities.read_files   Enable skill discovery for this toolset.
  on_messages(messages, ctx)
                            Derive state from the active conversation.
  render(opts)              Return semantic presentation data.

`details.changed_paths` tells the built-in Neo Controller which unmodified
buffers may be refreshed after a successful disk mutation.

Bundled presets: >lua

  require("neoagent.tools").coding()
  require("neoagent.tools").read_only()
  require("neoagent.tools").update_plan()
  require("neoagent.tools").all()
<
The coding preset provides file read, write, edit, shell, and Neoagent
documentation tools. File tools operate on disk. The optional plan tool derives
its current state from successful calls in the active Session path.

Bundled tools use `ctx.fs` and `ctx.process(argv, opts)` when supplied.
The filesystem capability implements `create_temp`, `read`, `mkdirp`, and
`write_all`. Process options include `cwd`, `env`, `clear_env`, `stdin`,
`capture`, `max_capture_bytes`, `timeout_ms`, `kill_grace_ms`, and
`on_output`. Results contain `code`, `signal`, `stdout`, `stderr`,
`output`, and `timed_out`.

The shell timeout defaults to 300 seconds. Set `shell_timeout = false` to
disable the default, or pass a positive `timeout` in the tool call. Captured
output is bounded; overflow is written through the filesystem capability.

`require("neoagent.session").new()` creates an in-memory Session. It accepts
`{ store = store }` or `{ messages = messages }`. The bundled store uses the
Pi v3 append-only JSONL format: >lua

  local store = require("neoagent.storage").new({
    directory = directory,
    cwd = cwd,
  })

  local session = require("neoagent.session").new({ store = store })
<
No file is created until the first message is accepted. Open an existing store
with `require("neoagent.storage").open(path)`.

Tree-aware Session methods are: >lua

  session:messages()
  session:context_messages()
  session:entries()
  session:entry(id)
  session:leaf_id()
  session:path([id])
  session:state()
  session:move_to(id[, summary])
  session:append_entry(type, values)
  session:label(id)
  session:name()
<
`move_to()` changes the active leaf; the next append creates a branch.
Create a linked child with: >lua

  local child = require("neoagent.storage").fork(store_or_path, {
    directory = directory,
    entry_id = user_entry_id,
    position = "before", -- or "at"
  })
<
`storage.list_sessions(directory, cwd)` returns recent picker records.
Workspace session indexes are disposable and rebuilt from the JSONL files when
missing or invalid. Reads and appends use per-file locks.

Reusable locks are available from `neoagent.file_lock`: >lua

  local lock = require("neoagent.file_lock").new({
    path = persistence_path .. ".lock",
    timeout_ms = 15000,
    poll_ms = 50,
    stale_ms = 120000,
  })
  local result = assert(lock:with(function() return update() end))
<
The parent directory must exist. `acquire()` returns an owned lease with
`release()`; `acquire_async()` is cancellable. Locks use token-validated
release and bounded stale recovery.

CONTEXT COMPACTION                                  *neoagent-compaction*

Automatic compaction runs near the Model's context limit and after a provider
context-overflow error. It stores a summary in the Session tree and uses that
summary before retained recent messages in later requests. The full tree
remains available for branch navigation.

The threshold is `context_window - reserve_tokens`; recent retention uses
`keep_recent_tokens`. Set `compaction = false` to disable all Controller
compaction or `compaction.auto = false` to keep manual and overflow
compaction.

A custom `compaction.run(opts)` returns a cancellable Run whose result
contains `summary`, `first_kept_entry_id`, `tokens_before`, and optional
`usage` and `details`. The `neoagent.compaction` module exposes
`settings`, `estimate_context`, `should_compact`, `prepare`,
`serialize`, and `run`.

Session adapters are: >lua

  require("neoagent.chat").send(session, prompt, opts)
  require("neoagent.chat").run(session, prompt, opts)
  require("neoagent.chat").continue(session, opts)
<
`continue()` uses projected context without adding a user message. The
optional `get_steering_messages` callback supplies user-message tables between
assistant/tool turns. Session adapters allow one active mutation per Session.

vim:tw=78:ts=8:noet:ft=help:norl:
